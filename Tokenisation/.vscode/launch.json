{
    "configurations": [
        {
            "name": "Launch Chrome",
            "request": "attach",
            "type": "pwa-chrome",
            "url": "http://localhost:3000",
            "program": "${workspaceFolder}\\client\\src\\app.js"
        }
  
    ]
}